# README — Base de Datos **mydb** (Aerolínea)

> Base orientada a operación de aerolínea: identidad & accesos, datos personales, geografía de aeropuertos, flota, rutas, programación y operación de vuelos, y tarifas por cabina.

---

## ░░ 1) ESTRUCTURA GENERAL (MÓDULOS) ░░
> Mapa mental por áreas con tablas clave.

### 🔐 Identidad & Accesos
- **persona**: entidad base para individuos (pasajeros, empleados, etc.).
- **usuario**: cuenta de acceso asociada a una *persona* y a un *rol*.
- **rol**, **permiso**, **rol_permiso**: control de acceso basado en roles y permisos (RBAC).

---

### 👤 Datos personales
- **direccion**, **telefono**, **documentacion**: info de contacto y documentos de la *persona*.

---

### 🌍 Geografía & Aeropuertos
- **pais**: catálogo de países.  
- **aeropuerto**: aeropuertos por país.  
- **terminal**: terminales por aeropuerto.  
- **puerta**: puertas dentro de una terminal.

---

### ✈️ Flota & Configuración
- **modelo_aeronave**: catálogo técnico (fabricante, capacidades).  
- **configuracion_cabina**: plantilla por modelo (filas y asientos por fila).  
- **aeronave**: aeronaves físicas (matrícula) asociadas a *modelo_aeronave*.  
- **asiento**: plano físico de asientos por *aeronave*.

---

### 🗺️ Red de Rutas & Operación
- **ruta**: tramos origen→destino (aeropuerto↔aeropuerto).  
- **vuelo_programado**: patrón (código, frecuencia, horas) sobre una *ruta*.  
- **vuelo**: instancia operativa (por fecha), con puerta de salida/llegada y aeronave asignada.

---

### 💵 Comercial & Precios
- **tarifa**: precio base por *ruta* y *clase* (cabina), con vigencia y cargos/impuestos simples.

---

## ░░ 2) ORDEN SUGERIDO DE EJECUCIÓN ░░
> Ejecuta los `.sql` en este orden para crear el esquema completo.

Esquema: 00_schema.sql
Catálogos base: pais.sql
Identidad & Accesos: persona.sql, rol.sql, permiso.sql, rol_permiso.sql, usuario.sql
Geografía: aeropuerto.sql, terminal.sql, puerta.sql
Flota: modelo_aeronave.sql, configuracion_cabina.sql, aeronave.sql, asiento.sql
Red & Operación: ruta.sql, vuelo_programado.sql, vuelo.sql
Comercial: tarifa.sql
Finalización: 99_finalize.sql

> Cada archivo ya incluye `USE mydb;` y `DROP TABLE IF EXISTS ...` para facilitar iteraciones.

---

## ░░ 3) RELACIONES CLAVE (VISIÓN RÁPIDA) ░░

- **usuario** → **persona**, **rol**  
- **rol_permiso**: many-to-many entre **rol** y **permiso**  
- **aeropuerto** → **pais** → **usuario** *(auditoría)*  
- **terminal** → **aeropuerto** → **puerta**  
- **aeronave** → **modelo_aeronave**  
- **asiento** → **aeronave**  
- **configuracion_cabina** → **modelo_aeronave**  
- **ruta** → **aeropuerto** *(origen/destino)*  
- **vuelo_programado** → **ruta**  
- **vuelo** → **vuelo_programado**, **aeronave**, **puerta** *(salida/llegada)*  
- **tarifa** → **ruta**

---

## ░░ 4) CASOS DE USO TÍPICOS ░░

### A) Agregar un **nuevo avión** (aeronave)
1. *(Opcional)* Crear/validar **modelo_aeronave** (p. ej., “A320”).
2. Insertar en **aeronave**: matrícula, fecha de alta, estado, `modelo_aeronave_id`.
3. Generar plano en **asiento**:
   - Basado en **configuracion_cabina** del modelo.
   - Insertar por cabina: `cabina`, `fila`, `columna`, `codigo_asiento`, flags *(bulkhead/salida)*.  
**Resultado:** la aeronave puede asignarse a un **vuelo** operativo.

---

### B) Crear un **nuevo usuario**
1. Crear **persona** *(nombre, apellido)*.  
2. Crear **usuario**: `email`, `password_hash`, `passwrod_algo` *(sic)*, verificación; asociar `rol_idrol` + `persona_idpersona`.  
3. *(Opcional)* Asignar permisos al rol vía **rol_permiso**.  
**Resultado:** el usuario puede autenticarse y operar según su rol.

---

### C) Definir una **nueva ruta** y **programar vuelos**
1. **ruta**: `origen_aeropuerto`, `destino_aeropuerto`, distancia/tiempo estimado.  
2. **vuelo_programado**: `codigo_vuelo`, `frecuencia` (p. ej., “L-M-X-J-V”), `hora_salida`/`hora_llegada`, `ruta_idruta`.  
3. *(Opcional)* **tarifa** por *ruta* y *clase* *(economica/ejecutivo/primera)*.  
**Resultado:** patrones listos para generar/operar instancias diarias.

---

### D) Operar un **nuevo vuelo** en una fecha concreta
1. **vuelo**:
   - `vuelo_programado_idvuelo_programado`, `fecha_operacion`  
   - `aeronave_idaeronave`  
   - `puerta_salida_idpuerta` *(y `puerta_llegada_idpuerta` si aplica)*  
2. Actualizar tiempos (`std/sta`, luego `etd/eta`, `atd/ata`) y `estado`.  
**Resultado:** vuelo publicado/operativo con infraestructura y equipo.

---

### E) **Nueva reserva** *(estado actual del esquema)*
> Aún no hay tablas de reservas/boletos/segmentos. Propuesta mínima para futuro:

- **reserva** *(PNR)*: id, localizador, `persona_contacto_id`, estado, `creado_en`, etc.  
- **reserva_item** / **segmento_reserva**: vincula *reserva* ↔ *vuelo* (uno por tramo):
  - `pasajero_idpersona`, `clase` (enum), `tarifa_id` *(opcional)*,  
  - `asiento_id` *(nullable hasta seat map)*, `estado` *(reservado/emitido/cancelado)*.  
- **pago** *(simple)*: `reserva_id`, monto, moneda, método, estado, timestamps.

> Con esto: crear reserva, cotizar con **tarifa** según *ruta/clase*, emitir pago y **asignar asiento** por segmento.

---

### F) **Asignar un pasajero a un vuelo** *(estado actual del esquema)*
> No hay tabla de cruce *persona↔vuelo*. Propuesta mínima:

- **pasajero_vuelo**:  
  - `vuelo_id` *(FK)*, `persona_id` *(FK)*  
  - `asiento_id` *(FK a **asiento**, opcional hasta la asignación)*  
  - `clase`, `estado_embarque` *(check-in, embarcado)*, timestamps.  

Hasta agregar estas tablas, la asignación no queda persistida.

---

## ░░ 5) ESTÁNDARES, AUDITORÍA Y BORRADO LÓGICO ░░
- Auditoría en la mayoría de tablas: `creado_en`, `actualizado_en`, `eliminado_en` + `*_por` *(FK a **usuario**)*.  
- Se favorece **borrado lógico** (`eliminado_en` + motivo) para preservar historia.  
- Booleanos como `TINYINT` *(0/1)*.

---

## ░░ 6) NOTAS Y LIMITACIONES CONOCIDAS ░░

**Typos conservados** *(para no romper DDL actual)*:
- `mdoelo` en **modelo_aeronave**  
- `timepo_estimado` en **ruta**  
- `es_whatssap` en **telefono**  
- `passwrod_algo` en **usuario**

**Índices a revisar**:
- **asiento**: `UNIQUE` independientes en `cabina`, `fila`, `columna`. Suele convenir  
  `UNIQUE (aeronave_idaeronave, cabina, fila, columna)`.
- **ruta**: `UNIQUE (origen_aeropuerto)`; en producción se usa típicamente  
  `UNIQUE (origen_aeropuerto, destino_aeropuerto)`.

**FK inválida**:
- En **rol_permiso**: `fk_rol_permiso_eliminado_por` sin columnas (debería apuntar a `usuario(idusuario)`).

**Reservas no modeladas**:
- Falta PNR/boletos/pagos/segmentos/asignación de asiento por pasajero (ver propuestas en §4E–4F).

---

## ✅ Checklist de próximos pasos
- [ ] Agregar módulo de **reservas** *(PNR, segmentos, pagos, pasajero_vuelo)*.  
- [ ] Ajustar **índices únicos** en `asiento` y `ruta`.  
- [ ] Corregir FK en `rol_permiso`.  
- [ ] Considerar `utf8mb4` y defaults de timestamps donde aplique.

---

> **Listo:** Con esto tenés una guía clara para navegar el esquema, ejecutarlo por módulos y operar los flujos más comunes (alta de flota, programación/operación de vuelos, usuarios y tarifas).
